<?xml version="1.0" encoding="UTF-8"?>
<!--
┌──┐
|  |
|Eh|ony
└──┘
-->
<features name="org.ehony">
    <feature name="${project.groupId}.${project.artifactId}" version="${project.version}">
        <bundle start="true">mvn:org.apache.geronimo.specs/geronimo-jms_1.1_spec/${org.apache.geronimo.specs.version}</bundle>

        <bundle start="false">mvn:${project.groupId}/${project.artifactId}/${project.version}</bundle>
    </feature>
</features>